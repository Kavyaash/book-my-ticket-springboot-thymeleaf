server:
  port: 8080

spring:
  application:
    name: book-my-ticket

  datasource:
    url: jdbc:mysql://localhost:3306/bookmyticket?createDatabaseIfNotExists=true
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      url: redis://localhost:6379

admin:
  email: admin@gmail.com
  password: admin1234
